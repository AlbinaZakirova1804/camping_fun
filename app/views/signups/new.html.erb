<%= form_for @signup do |f| %>
  <%= f.label :camper %>
  <%= f.collection_select :camper_id, @campers, :id, :name %>
  <%= f.label :activity %>
  <%= f.collection_select :activity_id, @activities, :id, :name %>
  <%= f.label :time %>
  <%= f.select :time, options_for_select(0..23) %>O'CLOCK
  <%= f.submit %>
<% end %>
